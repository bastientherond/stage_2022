<form method="post" action="{{ path('utilisateur_delete', {'iduser': utilisateur.iduser}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisateur.iduser) }}">
    <button class="btn">Delete</button>
</form>
